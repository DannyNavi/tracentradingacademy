/** Dice scramble animation length (ms). Token travel uses half of this. */
export const DICE_ROLL_DURATION_MS = 950;

/** Character portrait move-through-tiles animation (ms). */
export const TOKEN_MOVE_DURATION_MS = DICE_ROLL_DURATION_MS / 2;
